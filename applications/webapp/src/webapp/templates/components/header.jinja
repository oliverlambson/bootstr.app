<div class="container mx-auto pt-2">
    <div class="flex flex-nowrap items-end group">
        <a href="/">
            <span class="font-rethink text-3xl font-bold pr-2">bootstr.app</span>
        </a>
    </div>

    <nav class="py-4">
        <a href="{{ url_for("home") }}" class="pr-2 text-blue-500 hover:text-blue-800 {% if request.path == "/" %}font-bold{% endif %}">Home</a>
        <a href="{{ url_for("about") }}" class="px-2 text-blue-500 hover:text-blue-800 {% if request.path == "/about" %}font-bold{% endif %}">About</a>
    </nav>
</div>
