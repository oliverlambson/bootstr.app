[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
app = "app:main"

[tool.uv.sources]
app = { workspace = true }
app2 = { workspace = true }
core = { workspace = true }
functionality2 = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
